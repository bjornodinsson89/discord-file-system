[phases.setup]
# Use the generic `nodejs` package to guarantee `npm` exists across Nixpkgs revisions.
nixPkgs = ["python311", "gcc", "nodejs"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "cd frontend && npm ci && npm run build && cd .."
]

[start]
cmd = "python bot.py"
